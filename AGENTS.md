# 취뽀(chwi-ppo) 취업 지원 워크벤치

공식 채용 공고와 사용자가 확인한 경험으로 `intake → discover → apply`를 수행한다. 신입·경력 모두 지원하며 일정 관리는 별도 `track`으로 한다. 사용자용 산출물은 한국어로 작성한다. 처음 사용하는 사람은 [README.md](README.md)를 읽는다.

이 파일이 프로젝트 규칙의 정본이다. Claude Code는 `CLAUDE.md`를 통해 이 파일을 읽고, Codex는 이 파일을 직접 읽는다.

## 설치 요청 처리

사용자가 이 저장소의 설치·다운로드·복제를 요청하면 Git 복제 또는 릴리스 ZIP 압축 해제까지만 수행한다. 대상 폴더에 `AGENTS.md`와 `README.md`가 있음을 확인하면 설치 완료다. 사용자가 별도로 요청하지 않은 npm 패키지 설치, 빌드, 테스트, `/intake`, 전체 저장소 분석은 실행하지 않는다. 설치 완료 뒤에는 프로젝트 최상위 폴더를 열고 필요할 때 `/intake <개인 자료 경로>`로 시작할 수 있다고 짧게 안내한다.

## 실행 순서

```text
원자료 ── intake ──> 사람용 프로필·경험 Markdown
                         │
채용 목록 ─ discover ────┼──> 공식 JD·적합도·지원 우선순위
                         │
선택 직무 ── apply ──────┴──> 근거 매핑·초안·검수·최종본
                                      │
                         track <──────┘
```

1. `intake`: 최초 정리는 요약 우선·작은 작업 단위로 수행하고 결과를 즉시 저장한다. 중단 시 기존 run의 완료 결과부터 재사용한다. 이후에는 새 자료·정정만 갱신하며 기존 프로필을 불필요하게 재분석하지 않는다.
2. `discover`: 자소설닷컴 등의 공고 목록에서 사용자 조건에 맞는 후보를 추리고, 기업별 세부 JD를 공식 검증해 추천한다. 수집한 범위와 미처리 후보를 기록한다.
3. `apply`: 선택·검증된 직무에 승인된 경험만 배치해 검수 전 초안을 먼저 전달한다. 중간 사실 검수는 사용자가 요청할 때만, 독립 최종 검수는 내용 확정 뒤 `final` 요청 때 수행한다.
4. `track`: 사용자가 일정 관리를 원할 때 검증된 공고와 작성·검수·제출 상태를 캘린더에 반영한다. 캘린더는 추천·작성의 선행 조건이 아니다.

`apply`는 compact packet과 `intake` 산출물을 읽기 전용으로 사용한다. packet에 이미 담긴 JD·claim을 확인하려고 원본 전체를 중복 읽지 않고, 실제 불일치 때만 해당 근거 위치를 확인한다. 소재가 부족한 문항만 `intake 갱신 필요`로 남기고 나머지는 진행한다.

새 상세 JD 분석은 [문장별 매칭 계약](docs/runtime/JD_MATCHING.md)에 따라 01의 요구 ID와 02의 경험 판정을 빠짐없이 연결하고 03에서 문항별 근거로 배정한다. 사람용 정본은 Markdown이며 구조 누락·claim 상태·변경 지문은 로컬 코드로 검사한다. 경험 인덱스에서 관련 근거만 읽고 변경 항목만 재판단한다. 최초 원문 분해의 완전성과 연결 타당성은 메인이 확인하며 구조 검사로 대체하지 않는다. 기존 분석·지원서는 일괄 변환하지 않는다.

## 계층과 역할

역할 분담을 설계하거나 위임 경계를 확인할 때만 [docs/AGENT_ARCHITECTURE.md](docs/AGENT_ARCHITECTURE.md)를 읽는다. 이미 문맥에 제공된 지침은 다시 열지 않는다.

| 워크플로 | 역할 | 결과 |
|---|---|---|
| intake | document-extractor, project-extractor, profile-synthesizer | `profile/PROFILE.md`, `profile/experiences/*.md` |
| discover | 목록 수집·후보 분류, posting-verifier, jd-analyzer, role-fit-checker | 검색 snapshot·후보 큐, 회사별 00~02 파일 |
| apply | evidence-matcher, draft-writer, 선택적 fact-reviewer, revision-editor, final-red-team-reviewer | 회사별 03~07 및 `최종/` |
| track | 결정론적 검증·동기화 스크립트 | React 캘린더와 진행 상태 |

스킬은 사용자 명령 단위의 오케스트레이터이고, 에이전트는 한 가지 판단 역할만 맡는다. Codex는 `.agents/skills/`와 `.codex/agents/`, Claude Code는 `.claude/skills/`와 `.claude/agents/`를 읽는다. 두 플랫폼의 동명 스킬과 역할 본문은 같은 내용을 유지한다. 날짜 변환·중복·글자수·형식·개인정보 패턴처럼 규칙으로 판정 가능한 작업에는 모델을 쓰지 않고 `scripts/`를 사용한다. apply의 형식 검사된 초안은 fact/final PASS나 `ready`로 승격하지 않는다.

기본 실행자는 현재 대화의 메인 모델이다. 간단한 추출·통합·소재매핑·적합도 판단·초안·수정은 메인이 끝낸다. Astra로 대화 중이면 Astra가 수행하며, Luna로 할 수 있는 일이라는 이유만으로 넘기지 않는다. 역할 이름은 책임 구분이지 호출 지시가 아니다.

별도 위임 계획·비용 계산·판단 보고 없이 메인이 바로 처리한다. 명백히 큰 독립 작업만 예외적으로 나누며 기본 동시 2개 이내로 한다. 독립 사실 검수와 제출 전 final은 사용자가 명시적으로 요청했을 때만 작성자와 분리한다. 명백한 자격 불충족은 공식 공통 요건과 한 줄 사유에서 멈춘다.

## 모델 정책

메인 모델과 추론 수준은 사용자의 선택을 따른다. 아래 표는 예외적으로 위임할 때만 적용하며, 메인 작업을 해당 모델에 강제로 이관하지 않는다.

| 등급 | Codex | 용도 |
|---|---|---|
| efficient | `gpt-5.6-luna`, `max` | 추출·검색·구조화·근거 매칭 |
| strategic | `gpt-5.6-sol`, `high` | 자격 모호성·직무 선택·작성·수정 |
| final-audit | `gpt-5.6-sol`, `xhigh` | 실제 제출 직전 독립 검수 |
| deterministic | 모델 없음 | 날짜·중복·스키마·글자수·민감정보 검사 |

- 역할 문서의 `model-tier`에 따라 서브에이전트 모델을 선택한다.
- `final-audit`는 초안 작성 문맥을 물려받지 않는 독립 에이전트로 실행하며 읽기 전용이다.
- Claude Code에서는 같은 등급을 사용 가능한 경량/고성능 모델로 대응한다. 특정 Claude 모델이 없다는 이유로 단계를 생략하지 않는다.
- 모델 호출을 병렬화할 때는 서로 다른 파일에 쓰는 독립 작업만 병렬 실행한다. 같은 산출물을 동시에 수정하지 않는다.
- 모든 서브에이전트에는 역할·입력/출력 경로·완료 조건·필요한 질문만 전달한다. 전체 대화나 모든 경험 폴더를 관례적으로 넘기지 않는다. 이미 문맥에 있는 지침·근거를 다시 수집하지 않는다.
- 확인 질문 하나나 형식 수정 때문에 파이프라인 전체를 다시 실행하지 않는다. 현재 단계의 명령과 재사용 기준만 [실행 계약 색인](docs/WORKFLOW_RUNTIME.md)에서 선택해 읽는다.

## 작업 범위와 완료

- 조회·설명·점검은 답변이나 근거 있는 지적까지다. 별도 요청 없이 파일 수정·상태 갱신·전체 워크플로를 시작하지 않는다.
- 일반 지원서 수정은 기존 본문·배정 근거에서 바로 시작한다. 저장 후 해당 문항의 형식/글자수 검사를 통과하면 전달하며, 초안에 자동 사실 검수나 final을 붙이지 않는다.
- 구현 요청은 변경 → 영향받는 실행/검사 → 이번 변경으로 발생한 오류 수정 → 관련 검사 재실행까지 진행한다. 새 실패나 변경이 없으면 같은 검사를 반복하지 않는다. 세부 테스트 범위는 필요할 때만 실행 계약 색인의 검증 절을 본다.
- 일정 갱신은 실제 변경분을 저장·동기화하고 요청한 항목이 생성 결과에 반영됐는지 확인하면 끝난다. 변경이 없으면 재생성하지 않는다.
- 미확인 사실·권한·외부 접근 문제가 해결에 필요하면 해당 부분과 필요한 입력만 보고한다. 관련 없는 작업까지 재시작하거나 끝없이 재시도하지 않는다. 완료 기준은 외부 제출·게시 권한을 넓히지 않는다.

## 데이터 정본

- 사람용 정본: `profile/PROFILE.md`, `profile/experiences/*.md`
- 원자료 처리 이력: `state/intake-manifest.json` (완료 검증 후에만 갱신)
- intake 중간 저장: `.work/intake/<run-id>/run.json`, 작업별 facts Markdown (추출 완료와 프로필 검증·반영을 구분)
- 검색 입력/후보: `.work/discover/<run-id>/`의 snapshot·criteria·queue (포털 후보와 공식 검증을 구분)
- 공고·캘린더용 구조 데이터: `data/opportunities.json`
- 회사별 산출물: `companies/<회사명>/<직무명>/`

개인 경험을 JSON만으로 저장하지 않는다. 각 경험은 Markdown 한 장으로 만들고, 사실마다 `claim-id`와 원문 근거를 표시한다. `apply`는 `검증됨` 상태의 사실만 사용할 수 있다.

공고 데이터는 React 화면에서 사람이 확인하므로 JSON 사용을 허용한다. 공식 URL·마감 원문·확인 시각이 없는 공고는 `공식 확인 필요` 상태로 두며 확정 마감처럼 표시하지 않는다.

## 게이트

```text
intake: 확인 필요 사실 존재 → 사용자 확인 전 verified 금지
discover: 공식 출처 없음/자격 모호 → needs-review
fit: 진행 | 주의 | 재검토
fact review (사용자 요청 시): PASS | REVISE | BLOCK
final audit: PASS | REVISE | BLOCK
```

`재검토`, `REVISE`, `BLOCK`을 무시해 해당 직무/문항을 승인하지 않는다. 관계없는 검증된 경험·다른 직무 작업은 계속할 수 있다. 실제 지원서 제출이나 외부 전송은 이 프로젝트의 자동화 범위가 아니며 사용자의 명시적 요청과 확인 없이 수행하지 않는다.

## 절대 원칙

1. **사실 창작 금지.** 원천 자료에 없는 경험·수치·성과·기간·역할을 만들지 않는다.
2. **상태 승격 금지.** 준비·진행·출시 전·검증 전을 완료나 성과로 바꾸지 않는다.
3. **수치 보존.** 원문 수치를 임의로 반올림하거나 개선 폭으로 재해석하지 않는다.
4. **본인 기여 분리.** 팀 성과와 사용자의 직접 판단·수행을 구분한다.
5. **공식 출처 우선.** 비공식 채용 사이트는 후보 발견에만 사용하며 마감·자격 확정은 공식 페이지로 한다.
6. **검수와 수정 분리.** 사용자가 요청해 실행한 fact-reviewer와 final-red-team-reviewer는 파일을 수정하지 않는다. revision-editor만 검수 지적 범위에서 수정한다. 기본 초안과 일반 수정은 별도 검수 릴레이 없이 진행한다.
7. **평가자 독해 우선.** 내부 파일명·상태값·약어를 지원서에 그대로 옮기지 말고 지원자가 한 판단과 행동으로 설명한다.
8. **개인정보 로컬 보관.** 이력서·성적표·실제 프로필·지원서·공고 진행 데이터는 `.gitignore` 대상이다.
9. **`00_JD.md` 보존.** 공식 원문과 사용자 메모가 담긴 입력 파일은 후속 단계가 덮어쓰지 않는다.
10. **외부 행동 금지.** 검색과 로컬 작성은 가능하지만 지원서 제출·메시지 전송·공개 게시를 자동 실행하지 않는다.
11. **제출 완료 추정 금지.** `submitted`는 사용자가 실제 제출을 명시적으로 확인한 경우에만 기록하고 확인 시각을 남긴다. 그 전에는 `ready`를 유지한다.
12. **검색 범위 보존.** 첫 페이지·일부 후보만 확인하고 전체 검색 완료라고 하지 않는다. 포털에서 관측한 모든 행은 보존·제외·추가확인 중 하나로 기록하고 남은 작업을 큐에 둔다. 접근 실패와 채용 없음은 다르다.
13. **경력 구분.** 지원 유형 미상은 신입이 아니다. 신입/경력 혼합 공고는 지원 경로를 분리하고, 필수 관련 경력과 인턴·연구·병행 경력을 구분한다.

## 지원서 파일 규칙

- 제출용 본문은 파일당 하나의 `text` 코드 블록에 둔다.
- 모든 수치와 검증 가능한 주장에는 `claim-id` 또는 원천 파일 위치를 추적표에 남긴다.
- apply packet에 속한 파일은 `apply-packet.mjs checkpoint` 한 번으로 형식·글자수·claim 추적을 검사한다. 독립 파일만 `node scripts/check-submission.mjs <파일> <제한>`으로 검사한다.
- 사용자가 정확한 분량을 요구하지 않았다면 최대 글자수 이내로 쓰며 임의로 제한을 꽉 채우지 않는다.
- “최종 문장 수정” 같은 일반 편집으로 final을 호출하지 않는다. 실제 제출 전 전체 검수·제출본 확정 의도가 분명할 때만 final-red-team-reviewer를 한 번 실행하며, 같은 입력·본문·보고서 hash의 PASS는 재사용한다.
- final은 제출에 영향 있는 사실·수치·기간·역할·상태 불일치와 문항/자격 미충족에 한정한다. 문체 취향, 근거 없는 가상 우려, 학술 재감사, 면접 대비는 REVISE 사유가 아니다. 면접 준비는 서류 합격 뒤 사용자가 명시적으로 요청할 때 별도로 진행한다.
- 실제 제출 직전 final PASS와 민감정보 검사를 통과해야 `최종/`에 복사한다.
