---
name: apply
description: 선택한 공식 JD에 검증된 사용자 경험을 배치해 검수 전 초안을 빠르게 작성하고, 사용자가 final을 요청할 때 독립 최종 검수를 실행한다.
---

# Apply

`/apply <회사> <직무> [문항N] [final]`. 원자료 정리는 intake의 책임이고, 캘린더 등록 여부는 작성의 선행 조건이 아니다.

## 입력 준비

1. 선택한 직무의 공식 JD·분석·적합성, 실제 문항 원문·글자수를 확인한다. 로그인 뒤에만 보이는 문항은 사용자에게 그 문항만 요청한다. 비슷한 회사 문항을 대신 만들지 않는다.
2. 자격 `needs-review/ineligible`, 적합성 `재검토`이면 해결할 조건을 밝히고 해당 직무의 제출용 작성을 보류한다. 다른 직무나 검증된 경험 작업까지 중단하지 않는다.
3. `apply-packet.mjs catalog`의 검증 사실 요약으로 문항별 claim을 배정한다. 적은 문항은 메인이 evidence-matcher 역할을 수행하고, 대규모 문항 묶음만 Luna Max에 넘긴다.
4. `03_소재매핑.md`와 `application-request.json`을 [apply 실행 계약](../../../docs/runtime/APPLY.md)의 형식으로 만든다. 근거가 부족한 문항만 필요한 사용자 사실을 확인하거나 intake 갱신으로 돌린다.
5. `apply-packet.mjs prepare`의 compact `packet.md`를 우선 읽는다. 검증용 JSON은 입력 전문/hash를 유지하지만 모델용 Markdown은 JD 분석·선택 claim·공통 표현 제한을 중복 없이 제공한다. 누락이나 실제 불일치가 있을 때만 연결된 JD/경험 원문 위치를 추가 확인한다. `draft`만 새로 쓰고, `reuse-draft`는 checkpoint된 검수 전 초안, `reuse-final`은 현재 입력·본문·보고서 hash가 유효한 final PASS다.

## 작성과 검수

- 소수 문항의 매핑·작성·수정은 메인이 수행하고, 큰 독립 묶음만 matcher/writer에 한 번에 맡긴다. 신입/경력 모두 실제 본인 역할·판단·결과가 드러나게 쓴다. 배정된 검증 claim에 없는 사실·수치·성과를 만들거나 준비·진행 상태를 완료로 높이지 않는다. unknown만 질문하거나 제외하고 나머지는 즉시 작성한다.
- `04_초안/`에 문항별 하나의 text 블록과 claim 추적표를 둔다. packet이 있으면 `apply-packet.mjs checkpoint` 한 번으로 형식·placeholder·글자수·claim 추적을 검사한다. `check-submission.mjs`는 packet에 속하지 않는 독립 파일만 검사한다. 사용자가 정확한 분량을 명시하지 않았다면 제한 이내로 쓴다.
- 형식 검사를 통과한 본문은 `검수 전 초안`이라고 분명히 알려 바로 전달한다. 기본 작성에서는 fact-reviewer, revision-editor, 자동 재검수 루프를 호출하지 않는다. 초안 체크포인트는 PASS·`ready`·최종본이 아니다.
- “최종 문장 좀 바꿔”를 포함한 일반 수정은 요청한 문항·부분만 직접 바꾸고 checkpoint만 갱신한다. 표현에 `최종`이 들어갔다는 이유로 final을 호출하지 않는다. 새 사실이 필요한 부분만 질문하거나 제외하며 다른 문항을 다시 쓰지 않는다.
- 사용자가 중간 사실 검수를 명시적으로 요청한 경우에만 작성자와 별개인 fact-reviewer(Sol High)를 호출한다. `record --stage fact` 기록은 선택 검수 이력일 뿐 최종 PASS가 아니다.
- humanizer-ko는 사용자가 명시적으로 요청했을 때만 최종 검수 전에 적용한다. 문체가 바뀐 문항만 checkpoint한다.

## final

사용자가 내용을 확정한 뒤 `final`, 실제 제출 전 전체 검수, 제출본 확정을 분명히 요청할 때만 실행한다. 공식 공고 상태/자격·문항 변경 여부를 확인하되 변경 없으면 기존 분석·매핑을 재사용하고 중간 fact 검수를 선행하지 않는다. 같은 입력·본문·보고서 hash의 final PASS가 남아 있으면 재호출하지 않는다.

final-red-team-reviewer(Sol XHigh) 하나가 작성 대화·이전 판정 없이 현재 전체 문항을 한 묶음으로 한 번 검수한다. 제출에 영향 있는 확인 가능한 결함인 공식 문항/자격 미충족, 사실·수치·기간·역할·상태 불일치, 문항 미충족, 문항 간 모순에 한정한다. compact packet과 인용된 verified claim을 우선하고 실제 불일치가 있을 때만 해당 원자료를 추가 확인한다. 문체 취향·근거 없는 가상 우려·연구 재현·학술 재감사는 REVISE 이유가 아니다. 예상 면접 질문이나 면접 방어성은 서류 합격 뒤 사용자가 면접 준비를 명시적으로 요청할 때 별도로 다룬다. 보고서에는 `검수 단계: final`, 입력 해시, 실제 본문 해시를 남긴다.

REVISE면 지적된 문항만 수정하고 새 주장·근거를 추가하지 않는다. checkpoint 뒤 이전 지적과 수정 영향만 한 번 재확인하며 자동 반복하지 않는다. 남은 결함은 사용자에게 보고한다. 모든 최종 파일이 현재 입력과 실제 본문 해시의 final PASS 범위에 속할 때만 민감정보 검사 후 `최종/`에 복사한다. `submitted`는 사용자가 실제 제출을 확인할 때만 기록한다.
